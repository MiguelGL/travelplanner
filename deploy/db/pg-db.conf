. ../defaults.conf

PG_USER=miguelgl
#PG_USER=postgres

PG_PATH=/usr/local/bin
#PG_PATH=/usr/lib/postgresql/9.5/bin
#PG_PATH=/usr/bin

#DB_HOST=/var/run/postgresql

PSQL_ARGS="--quiet --single-transaction --username ${DB_USER} --host ${DB_HOST} --dbname ${DB_NAME}"
